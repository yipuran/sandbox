package org.yip.sandbox;

//import org.yipuran.rank.RankAccumulator;

/**
 * RankTest1
 */
public class RankTest{
	public static void main(String[] args){

//		List<User> list1 = UserFactory.creeate3().stream()
//		.sorted((a, b)->Integer.valueOf(b.point).compareTo(Integer.valueOf(a.point)))
//		.collect(()->new ArrayList<User>(), RankAccumulator.seek(t->t.point, (t, u)->t.rank=u), (r, u)->{});
//
//		System.out.println("======= seek ===========");
//		list1.stream().forEach(e->{
//			System.out.println(e.name + ":" + String.format("%5d", e.point) + " : " + e.rank );
//		});
//
//		List<User> list2 = UserFactory.creeate3().stream()
//		.sorted((a, b)->Integer.valueOf(b.point).compareTo(Integer.valueOf(a.point)))
//		.collect(()->new ArrayList<User>(), RankAccumulator.skip(t->t.point, (t, u)->t.rank=u), (r, u)->{});
//
//		System.out.println("======= skip  ===========");
//		list2.stream().forEach(e->{
//			System.out.println(e.name + ":" + String.format("%5d", e.point) + " : " + e.rank );
//		});

	}
}
